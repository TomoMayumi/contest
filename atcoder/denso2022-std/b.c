#include <stdio.h>

int main(){
    int n,k;  // 各メダルの枚数
    int n;                // メダルの総数

    // 入力
    scanf("%d %d", &n, &k);

    // Pを
    sum = gold + silver + bronze;

    // 出力
    printf("%d\n", sum);

    return 0;
}
